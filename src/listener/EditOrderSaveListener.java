package listener;

import java.awt.event.*;

/**
 * @author Sophie Orth, Monica Alessi, Dhruv Aggarwal, Maik Fichtenkamm, Lucas
 *         Lahr
 */
public class EditOrderSaveListener implements ActionListener {

    /**
     * {@inheritDoc}
     */
    @Override
    public void actionPerformed(ActionEvent e) {
        // if (checkOrderValidity()) {
        // saveEditedOrder();
        // if (create != true) {
        // MainPanel.getNavPane().setComponentAt(6,
        // new NavItemPanelChooser("Logistik", "ShowOrder", null));
        // } else {
        // MainPanel.getNavPane().setComponentAt(6, new NavItemPanelChooser("Logistik",
        // null, null));
        // }
        // } else {
        // JOptionPane.showConfirmDialog(editPanel, "Please fill out ALL fields
        // correctly?");
        // saveButton.setEnabled(false);
        // }

        // }

    }

}
